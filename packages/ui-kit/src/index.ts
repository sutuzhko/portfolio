/**
 * Публичный API `@sutuzhko/ui-kit`.
 * Компоненты дизайн-системы + утилиты, на которые опирается их API.
 * Стили подключаются отдельно: `import '@sutuzhko/ui-kit/styles.css'`.
 */

export * from './ui';
export { cn, type ClassValue, initials } from './lib';
