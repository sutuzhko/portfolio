/**
 * Константы навбара. Бренд-имя и глиф консоли — не переводимый технический
 * контент, поэтому живут в конфиге виджета рядом с ним, а не в i18n
 * (там только озвучиваемые подписи).
 */

/** Имя в бренд-марке центра навбара (обрамляется как `<name />`). */
export const BRAND_NAME = 'bogdan.sutuzhko';

/** Глиф кнопки консоли, как в макете. */
export const CONSOLE_GLYPH = '>_';
