import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';

import * as Stories from './contributor-manager.stories';

<Meta of={Stories} />

# Widgets / AdminProjects / ContributorManager

Управление участниками (коллабораторами) прямо в форме проекта. Co-located здесь вся
работа с общим каталогом контрибьюторов: **выбор** участников в проект (чипы-тогглы),
**порядок** (перетаскивание), **создание**, **правка** и **удаление** каталожных записей.
Форма проекта лишь прокидывает выбранные id и колбэки черновика — вёрстка и состояние
редактора живут тут.

---

## Playground

<Canvas of={Stories.Default} />

<Controls />

---

## Состояния

### Перестановка

Ручка слева от чипа перетаскивает участника — мышью/тачем или с клавиатуры (пробел —
взять, стрелки — сдвинуть, пробел — положить). Порядок каталога **глобальный**: в нём же
участники идут на публичных плитках, в детали проекта и в превью плитки справа. На
сохранении проекта уходит минимальный дифф — PATCH `order` только у сдвинутых (дробная
позиция между соседями, как у технологий).

<Canvas of={Stories.Reordering} />

### Создание

Кнопка «+ создать участника» разворачивает инлайн-форму (имя в активной локали, цвет
аватара, опциональная ссылка). После успеха новый участник сразу выбирается в проекте.

<Canvas of={Stories.Creating} />

### Правка

У каждого чипа — карандаш: открывает ту же форму, предзаполненную значениями записи, с
кнопкой «Удалить» (через `ConfirmDialog`).

<Canvas of={Stories.Editing} />

### Заблокировано

Пока сохраняется проект, ручки, карандаши и «+ создать участника» неактивны.

<Canvas of={Stories.Disabled} />

---

## Локализация имени

Имя правится в **активной локали**, но бэкенд **перезаписывает** его целиком
(`writeText`), поэтому в тело запроса уходят **обе локали**: активную берём из формы,
вторую несём из текущей записи — перевод не теряется. Для нового участника, введённого
в EN, дублируем значение в обязательный `ru`.

## Ответственность

Чипы-выбор + перестановка + инлайн-CRUD каталога + подтверждение удаления. **Не отвечает** за сетевые
запросы (контейнер `AdminProjects`) и за остальную форму проекта (`ProjectForm`).
